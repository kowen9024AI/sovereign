{"authority":"NONE","mission_id":"mission-sscm-01b-live-2","notes":"Topology is host-frozen: exactly two tasks, unchanged task_id, target_role, allowed_files, expected_content, and commit_required. Coordinator authority is NONE; no tools were run. Each worker operates only on its own isolated branch and its single allowed file.","objective":"Produce a bounded two-task fan-out plan. The topology is frozen by the host: exactly the two tasks below, each creating one file with exact content and committing on its own isolated branch. Do not add, remove, or reorder tasks.","role":"COORDINATOR","schema_version":"sovereign.sscm-coordinator-fanout-plan.v0.1","tasks":[{"allowed_files":["docs/sscm-fanout-a.txt"],"authority":"NONE","commit_required":true,"expected_content":{"docs/sscm-fanout-a.txt":"SSCM_FANOUT_A_OK\n"},"objective":"On your own isolated branch, create the file docs/sscm-fanout-a.txt containing exactly the line SSCM_FANOUT_A_OK followed by a single trailing newline, and commit that one file. Do not touch any other path.","target_role":"WORKER_A","task_id":"task:fanout-a"},{"allowed_files":["docs/sscm-fanout-b.txt"],"authority":"NONE","commit_required":true,"expected_content":{"docs/sscm-fanout-b.txt":"SSCM_FANOUT_B_OK\n"},"objective":"On your own isolated branch, create the file docs/sscm-fanout-b.txt containing exactly the line SSCM_FANOUT_B_OK followed by a single trailing newline, and commit that one file. Do not touch any other path.","target_role":"WORKER_B","task_id":"task:fanout-b"}],"verification_requirements":["Lane A (task:fanout-a, WORKER_A): the host runs git diff --name-only <base>..<lane-a-branch> and confirms the only changed path is docs/sscm-fanout-a.txt.","Lane A: the host runs git show <lane-a-head>:docs/sscm-fanout-a.txt and confirms the bytes equal exactly \"SSCM_FANOUT_A_OK\\n\" (single trailing newline, no other content).","Lane A: the host confirms at least one commit exists on the lane-a branch ahead of the base (git rev-list --count <base>..<lane-a-branch> >= 1) and the worktree is clean (git status --porcelain is empty).","Lane B (task:fanout-b, WORKER_B): the host runs git diff --name-only <base>..<lane-b-branch> and confirms the only changed path is docs/sscm-fanout-b.txt.","Lane B: the host runs git show <lane-b-head>:docs/sscm-fanout-b.txt and confirms the bytes equal exactly \"SSCM_FANOUT_B_OK\\n\" (single trailing newline, no other content).","Lane B: the host confirms at least one commit exists on the lane-b branch ahead of the base (git rev-list --count <base>..<lane-b-branch> >= 1) and the worktree is clean (git status --porcelain is empty).","Integrated candidate: the host merges both lane branches onto the base and confirms the merge completes with no conflicts (no unmerged paths in git status).","Integrated candidate: the host runs git diff --name-only <base>..<candidate> and confirms the changed paths are exactly docs/sscm-fanout-a.txt and docs/sscm-fanout-b.txt, nothing more.","Integrated candidate: the host runs git show <candidate>:docs/sscm-fanout-a.txt and git show <candidate>:docs/sscm-fanout-b.txt and confirms each matches its expected_content byte-for-byte.","Integrated candidate: the host confirms git log <base>..<candidate> contains the lane A and lane B commits (plus at most one merge commit) and no commits from any other source."]}
